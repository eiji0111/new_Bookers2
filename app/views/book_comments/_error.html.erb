<% if comment.errors.any? %>
  <div class="error_message alert alert-warning">
    <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li class="fas fa-exclamation-triangle"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>