<div class="container">
  <div class="row justify-content-center">
    <div class="col text-center m-3">
      <h2>本当に退会しますか？</h2>
      <p>退会する場合は、『退会する』をクリックしてください。</p>
        <%= link_to '退会しない', user_path(@user), class: 'btn btn-primary btn-sm' %>
        <%= link_to "退会する", withdraw_user_path(@user), method: :patch, class: 'btn btn-danger btn-sm' %>
    </div>
  </div>
</div>